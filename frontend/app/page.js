export default function HomePage() {
  return (
    <div>
      <h1>Blog Frontend</h1>
      <p>
        A Next.js App Router app using NextAuth. Sign in to view your profile —
        credentials are checked against the Blog-App Express backend.
      </p>
    </div>
  );
}
